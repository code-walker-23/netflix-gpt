import React from "react";
import Header from "./Header";

const Browse = () => {
  return (
    <div className="browse relative h-screen bg-black">
      <Header />
    </div>
  );
};

export default Browse;
